services:
  agy-cli-proxy:
    build: .
    image: ghcr.io/arocial/agy-cli-proxy:latest
    ports:
      - '3403:3403'
    environment:
      - PORT=3403
    volumes:
      - ~/.gemini:/root/.gemini
    restart: unless-stopped
